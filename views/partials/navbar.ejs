<nav class="bg-gradient-to-r from-blue-600 to-purple-600 text-white shadow-lg">
    <div class="container mx-auto px-4 py-3 flex flex-col md:flex-row justify-between items-center space-y-2 md:space-y-0">
      <a href="/" class="text-2xl font-bold hover:text-blue-200 transition-colors">
        🎓 Smart Notice Board
      </a>
      
      <div class="flex items-center space-x-4">
        <% if (user) { %>
          <div class="hidden md:flex items-center space-x-2">
            <span class="text-sm"><%= user.username %></span>
            <span class="px-2 py-1 text-xs font-bold rounded-full 
              <%= user.role === 'admin' ? 'bg-red-500' : 
                 user.role === 'teacher' ? 'bg-green-500' : 'bg-blue-500' %>">
              <%= user.role.toUpperCase() %>
            </span>
          </div>
          <a href="/logout" class="bg-white text-blue-600 px-4 py-2 rounded-lg hover:bg-gray-100 transition-colors">
            Logout
          </a>
        <% } else { %>
          <div class="flex space-x-2">
            <a href="/login/admin" class="bg-red-500 text-white px-4 py-2 rounded-lg hover:bg-red-600 transition-colors">
              Admin Login
            </a>
            <a href="/login/teacher" class="bg-green-500 text-white px-4 py-2 rounded-lg hover:bg-green-600 transition-colors">
              Teacher Login
            </a>
            <a href="/login/student" class="bg-blue-500 text-white px-4 py-2 rounded-lg hover:bg-blue-600 transition-colors">
              Student Login
            </a>
            <a href="/register" class="bg-purple-500 text-white px-4 py-2 rounded-lg hover:bg-purple-600 transition-colors">
              Register
            </a>
          </div>
        <% } %>
      </div>
    </div>
  </nav>
  